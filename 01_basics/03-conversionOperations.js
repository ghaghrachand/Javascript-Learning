let age = 27

console.log(typeof(age));
let valueInNumber = String(age)
console.log(valueInNumber);
console.log(typeof(valueInNumber)) 

//like wise we can convert all the data types but JS is bit confusing in converting the values so all the cross chk the values whether it's boolean,number,string,undefined or anything